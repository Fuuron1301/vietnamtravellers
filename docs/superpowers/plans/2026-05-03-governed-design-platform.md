# Governed Design Platform Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add governance-as-code that prevents UI drift and enforces the Asia Pioneer SEO skeleton, Asiatica booking flow, and LuxTravel-inspired visual discipline.

**Architecture:** Create design-system contracts, registry, page blueprints, visual fingerprints and a validator script. Update tokens to the strict scale and wire governance into package scripts.

**Tech Stack:** TypeScript design-system modules, Node.js validation script, Tailwind CSS tokens, Next.js frontend.

---

### Task 1: RED Governance Checks
- [ ] Add a smoke fixture with known invalid utility usage.
- [ ] Run `npm run governance:check` and confirm it fails because the script does not exist yet.

### Task 2: Governance Modules
- [ ] Create `design-system/governance-rules.ts`.
- [ ] Create `design-system/contracts.ts`.
- [ ] Create `design-system/component-registry.ts`.
- [ ] Create `design-system/page-blueprints.ts`.
- [ ] Create `design-system/visual-fingerprint.ts`.

### Task 3: Validator Script
- [ ] Create `scripts/design-governance-check.mjs`.
- [ ] Add `governance:check` and `verify` scripts to `package.json`.
- [ ] Ensure invalid fixtures are detected.

### Task 4: Token Alignment
- [ ] Update `lib/design-tokens.ts` to strict 8pt grid values.
- [ ] Update `tailwind.config.js` spacing and typography to match governance rules.
- [ ] Fix component classes that violate spacing/arbitrary-value rules.

### Task 5: Documentation and Verification
- [ ] Add docs under `docs/design-system`.
- [ ] Run `npm run governance:check`.
- [ ] Run `npm run typecheck`.
- [ ] Run `npm run lint`.
- [ ] Run `npm run build`.
